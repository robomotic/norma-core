[project]
name = "st3215-calibration-dump"
version = "0.1.0"
description = "Dump ST3215 per-motor calibration state (range_min/max, EEPROM offset) for debugging"
requires-python = ">=3.10"
dependencies = []
